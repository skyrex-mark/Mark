{
  "name": "mark",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "logrocket": "^0.6.17",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-particles-js": "^2.4.2",
    "react-scripts": "2.1.3",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^0.84.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "test:ci": "set CI=true && react-scripts test --env=jsdom",
    "test:coverage": "set CI=true && react-scripts test --env=jsdom --coverage  --coverageDirectory=coverage",
    "eject": "react-scripts eject",
    "docz:dev": "docz dev",
    "docz:build": "docz build"
  },
  "devDependencies": {
    "docz": "^0.12.15",
    "husky": "^1.3.0",
    "nyc": "^13.1.0",
    "prettier": "^1.16.3",
    "pretty-quick": "^1.8.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged && yarn test:ci"
    }
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
